apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: monitoring
  labels:
    app: podinfo
spec:
  ports:
  - port: 9898
    targetPort: http
    name: http
  selector:
    app: podinfo
